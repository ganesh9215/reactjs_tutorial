“useFetch” — a Custom Hook for API Calls

Let’s build a custom hook to fetch data from an API (e.g., users or products) and handle:

Loading state

Error handling

Data caching

Then we’ll reuse it in multiple components — just like in real projects.
