We’ll build a mini chat window where:

Messages appear one by one (like in WhatsApp / Slack)

We always scroll to the latest message automatically

useRef is used to:

Access the chat container DOM node

Keep track of message list end

Avoid unnecessary re-renders
